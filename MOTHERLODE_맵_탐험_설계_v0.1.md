# MOTHERLODE — 맵·탐험 설계 v0.1 (마름모 진격 지오메트리 + 절차생성 + 배치 + 지형)

> 상위: `기획서_v1.0` §8(맵·탐험)·`작성계획서` 파트F·`적_설계_v0.1`·`적_패턴심화_v0.2`(§1.4 아레나=패턴).
> 목적: **"어케 만드냐"** 에 답한다. 마름모 확정 + Dormans식 3단 절차생성 + 적/엘리트/미니보스/보스/유물함 배치 + **도형이 아니라 전략·탐험 가치를 가진 지형**.
> 깊이: 지오메트리·생성 파이프라인·배치 규칙·검증 = **[확정]**. 회로 모듈 라이브러리·지형 셰이프 그래머 규칙 세트 = **[원칙+예시]**, 구현값은 후속 `절차생성_알고리즘`.

---

## 0. 동심원 → 마름모 (확정)

**동심원 폐기.** 동심원은 **수성(웅크림) 지오메트리** — 본부를 링이 감싸 "거리=위험"을 방어적으로 표현. 우리는 **공격 중심 페이싱**(초반 수성→후반 공세)이라 **진격 지오메트리**가 필요하다.

**마름모 확정.**

```
            B (적 최종보스)
           ╱ ╲          ← 외곽 밴드(난이도 최고)
       L ╳     ╳ R      ← 허리(waist): 넓은 격전지 + 측면 루트
           ╲ ╱
            A (아군 본부)   ← 근거리 밴드(난이도 최저)
```

- **대각 주축 A→B** = 난이도 구배 = access-level(A에서 그 노드까지의 자물쇠 수). 본부 끝 낮음 → 보스 끝 높음. 동심원의 링을 **선형 전선**으로 옮긴 것.
- **좌우 꼭짓점 L·R / 허리** = 측면 루트 + 넓은 격전지(다대다). **주축 외 ≥1 루트**를 보장해 교착 방지.
- **거리=위험을 안개로** 표현(이전 합의): 멀수록 안 보임 = 탐험 비용.

---

## 1. 어케 만드냐 — 3단 절차생성 (미션 → 공간 → 지형+검증)

> Dormans 미션-공간 분리 + lock&key + access-level + 다단 검증. 논리를 먼저, 공간은 나중, 지형은 마지막, 검증은 매 단계.

### 1.1 [1단] 미션 그래프 — 논리 골격 (lock & key)

먼저 **"무엇이 무엇을 막나"** 를 그래프로 짠다. 공간 없이 논리만.

- **노드:** 본부(start) · 보스(goal) · 적 거점 · 미니보스(=자물쇠) · 유물함 · 교두보 · 측면 게이트.
- **엣지:** 이동(regular) / 게이팅(unlock — 자물쇠↔열쇠).
- **백본 = 미끼 C 회로**를 grammar 백본으로: `본부 → 문지기 → 유혹 → 성장 → 전설마커 → (자물쇠)우회 → 전설 → … → 보스`. 회로 모듈 라이브러리에서 조립·재배열(그래프 재작성).
- **access-level = 난이도 구배.** 보스 = 최고 access-level. 각 미니보스(자물쇠)가 access-level을 +1.

### 1.2 [2단] 공간 임베딩 — 그래프 → 마름모 2D

논리 그래프를 마름모에 앉힌다.

- **축 배치:** access-level → A→B 대각 위치(낮음=A끝, 높음=B끝). 단조 증가(검증 §4).
- **측면 비대칭:** 좌우로 흩어 측면 루트·우회 생성. 대칭이되 동일하지 않게(스탬프 느낌 제거).
- **유기적 분포:** 완화 Voronoi / Poisson-disk 간격 — **격자 타일 아님**(성의 §6).
- **엣지 = 물리 루트:** 그래프 엣지를 협로·회랑·개활지로 물질화. **지형이 그래프를 몸으로 만든다.**

### 1.3 [3단] 지형 살붙이기 + 검증 (shape grammar + validation)

- 각 지역에 §2 지형 프리미티브를 셰이프 그래머로 배치 — 지역마다 전략 성격 부여.
- §4 검증 패스 전부 통과할 때까지 **재생성**(다단 분리라 빠른 재시도 — Dormans 이점).

---

## 2. 지형 프리미티브 — 도형이 아니라 전략·탐험 가치 (성의의 핵심)

> 사용자 요구: "단순 도형 배치 ✗". 그래서 각 지형은 **명확한 전략 동사 + 탐험 가치 + 전투 깊이(§1.4) 연결**을 가진다. 규칙 수는 적게(7종), 조합은 많게(간단하지만 깊은).

| 지형 | 전략 동사 | 탐험 가치 | 전투 깊이 연결 | 렌더(제한 그래픽) |
|---|---|---|---|---|
| **협로** | 방어 우위·개떼 깔때기·루트 통제 | 너머에 보상 은닉 | 적은 수로 라인홀드 | 두 불통과 덩어리의 **음(陰)의 공간** |
| **엄폐물** | 시야·원거리 차단(교전 전 위치값)·매복 | 유물·매복 은닉 | 스나이퍼 LoS 끊기, 접근 안전 | 점묘 패치 / 부서진 외곽 |
| **고지대** | 시야(안개 너머)+사거리·통제점 | 정찰 우위 | 통제권 다툼의 표적 | 밝은 채움 + 또렷한 절벽선 + 램프 |
| **개활지** | 빠른 이동 but 노출·대규모 전투장 | 가로지르는 도박 | 다대다 합성 필드(§1.5) 무대 | 빈 바닥(대비로 위험 전달) |
| **위험지형** | 자연 벽·링아웃 가장자리·루팅 강제 | 위험 게이트 보상 | 가장자리 압박(§1.4) | 일렁이는 셰이드(물/용암/균열) |
| **폐허·교두보** | 전진 거점·자원토템 앵커·스왑노드 | 발 딛는 곳·재건 | 부활 인접·전선 전진 | 부서진 사각 외곽 + 재건 게이지 |
| **파괴지형** | 지름길 개방(대가)·보스가 엄폐 파괴 | 위험-보상 우회 | 지형 파괴(§1.4)로 학습 위치 무효화 | 금 간 덩어리 + 파괴 시 잔해 |

**남용 금지(통설):** 협로를 사방에 깔면 단조·예측. 협로는 **요충에만**, 나머지는 완만한 슬로프·곡선 절벽으로 부드럽게 보호. 허리엔 3v3+급 **넓은 개활 격전지**를 최소 1개 두고 램프·언덕·계곡으로 변주.

**조합 = 국소 퍼즐 (성의의 출처):**
- 협로 + 고지대 = **요새**(점령 가치 큰 길목).
- 개활지 + 위험 가장자리 = **죽음의 도하**(빠르나 링아웃 위험).
- 엄폐물 + 유물함 = **매복 리스크 보상**(앉아 루팅할까, 노출될까 — 통설의 risk/reward).
- 파괴지형 + 협로 = **부수면 우회로**(시간·피해 대가).

> **귀중 지역이 앞으로 끈다(통설):** 고지대·유물·자원토템을 주축 앞쪽에 배치해 플레이어를 전진·충돌로 유인 → "웅크리면 진다, 전진 강제" 페이싱의 엔진.

---

## 3. 배치 규칙

### 3.1 적 거점 (공습원)
- 밴드별로 주축·루트 길목에 — **협로·고지대 뒤**(방어적 출제 위치)에 앉음. 각 거점이 한 루트 구간을 지킴.
- 깨어나면 연결 루트로 **개떼를 A 방향 송출**. 거리·강도 ∝ 밴드(access-level).
- **순차 각성 = 공간 게이팅:** 일부 거점은 "열쇠" — 파괴 시 다음 밴드 각성/개방. (구 "순차 각성"을 동심 시간이 아니라 **진격 진행**으로 재구현.)
- 파괴 = 그 루트 러쉬 차단 + 보상.

### 3.2 엘리트
- **필드 배회형:** 개활 밴드를 순찰 → 우연 조우로 다대다 발생.
- **수문장형(삼중주):** 협로·고지대·거점 접근로에 2~3기 주둔 = 미끼 C의 **문지기**. 합성 필드(§1.5)로 관문을 지킴.

### 3.3 미니보스 (지역 자물쇠)
- **지역 락:** 각 미니보스가 고가치 지역(유물 금고 / 확장 군집 / B로 가는 측면 지름길) 접근을 게이팅.
- 자기 지역 내 **배회**(A로 안 옴 — 가서 깬다). 처치 = 지역 해금 + access-level 통과(Boss Keys식 게이팅).
- access-level 전환점에 배치. 미끼 C의 **전설마커 앞 자물쇠**가 대표 위치.

### 3.4 적 최종보스
- **마름모 대각 끝 B.** 난이도 구배의 정점. **밴드3 거점 + 외곽 미니보스 ≥1** 뒤에 게이팅.
- 아레나 자체가 지형을 씀(§1.4): 고지대·가장자리·파괴 가능 엄폐.

### 3.5 유물함 (미끼 C 회로 = 배치 로직)
미끼 C 회로가 곧 유물 배치 문법이다.

| 회로 단계 | 배치 | 유물 등급 | 리스크 게이트 |
|---|---|---|---|
| 문지기 | 관문 엘리트/미니보스 | — | 통과 시험 |
| 유혹 | 루트 가까이·안개 살짝 너머 | 일반(보너스 칸) | 낮음 |
| 성장 | 더 깊은 지역 | 희귀 | 엘리트 조우 |
| 전설마커 | 안개 뚫는 **비콘**(멀리서 보임) | (표식) | 유인 |
| 우회 | 측면 루트 / 자물쇠 처치 후 | — | 우회 비용 |
| 전설 | 위험지형·자물쇠 뒤 | 전설 | 높음(도박수) |

- **리스크 게이트 등급:** 일반=루트변(저위험), 희귀=엄폐·소수 수문장 뒤, 전설=미니보스 락/위험 도하 뒤. **귀중할수록 전진을 강제.**

### 3.6 자원 토템
- **본부·교두보에만**(작업로그 확정). 전진해 교두보를 재건·점령해야 토템 = 돈 → **전진 경제**. 맵 통제 = 경제.

---

## 4. 검증 패스 (절차생성이 쓰레기 안 되게)

다단 분리의 진짜 값어치 — 각 패스 실패 시 해당 단계만 재생성.

1. **연결성:** 모든 필수 노드가 A에서 도달 가능.
2. **난이도 단조:** access-level이 A→B 단조 증가. 보스 옆 쉬운 노드·본부 옆 잔혹 노드 금지.
3. **공정 러쉬 거리:** 최근접 밴드0 거점이 **첫 교두보 확보 전에 A를 못 무너뜨림**(개막 0~5분 페이싱). 최소 거리/각성 지연.
4. **만회 경로:** 뒤처진 플레이어용 **고위험-고보상 경로 ≥1**(데스 스파이럴 방지·도박수). 예: 위험지형 게이트 지름길, 밴드 스킵 측면.
5. **다중 루트:** A→B **≥2 루트**(주축 + 측면 ≥1). 단일 협로가 진행을 하드락하지 않음(교착 방지).
6. **다중해(헌법8):** 보스·핵심 자물쇠가 대표 빌드 3종으로 클리어 가능.
7. **탐험 보증:** 미끼 C 회로 존재·해결 가능. 안개 점진 해소. 유인용 전설마커 ≥1 가시.

---

## 5. 안개 + 탐험 (미끼 C 작동)

- **안개 = 전장**(내 정보 아님). 유닛·시야원 근처만 보임. 멀수록 안 보임 = 거리-위험.
- **전설마커 = 비콘:** 안개 뚫고 멀리서 빛남 = **유혹**. 위험을 알면서도 끌리는 당근.
- **정찰:** 고고학자 = 안개 해소 특화. 고지대 점령 = 광역 시야.
- **공정(헌법 9):** 안개는 맵을 가리되, **교전 진입 시 텔레그래프는 100% 노출.** 지형·안개로 예고를 숨기지 않음.

---

## 6. 성의 + 제한 그래픽 (지형판)

텔레그래프 문법과 같은 해법: **일관된 지형 시각 언어 + 유기적 배치 + 기능 가독성.**

- **지형도 문법으로 읽힌다:** 색+형태+엣지로 종류 구분(고지=밝은 채움+절벽선, 엄폐=점묘, 위험=일렁임, 협로=음의 공간). 한 번 배우면 모든 맵에 통함.
- **유기적 배치(Voronoi/Poisson)** = 격자 타일의 "찍어낸" 느낌 제거 → "설계된" 인상.
- **엣지·실루엣이 성의를 진다:** 해안선·절벽 윤곽 한 줄이 싸지만 강한 인상. 채움보다 경계가 일함.
- **기능이 곧 성의:** "이 협로가 왜 중요한지 보이는 것" 자체가 공들임. 장식 지형 0개 — 모든 지형이 전략 동사를 가짐.

---

## 7. 구현 메모 / 다음

**구현**
- 3단 전부 데이터 주도: 그래프 규칙·임베딩 제약·셰이프 그래머·검증 술어를 외부 데이터로.
- 지형 충돌·LoS·고지 사거리는 프로토 v5 지형지물 위에 확장. 안개 = 시야 마스크.
- 검증 = 자동 술어(연결성=BFS, 단조=access-level 정렬, 다중루트=경로 2개 존재, 다중해=대표 빌드 시뮬). 실패 시 재생성.

**다음**
- `절차생성_알고리즘`: 회로 모듈 라이브러리 실데이터 + 그래프 재작성 규칙 + 셰이프 그래머 규칙.
- §1.4(아레나=패턴)와 보스 아레나 결합: 보스별 지형 처방.
- 엘리트 삼중주 ↔ 수문장 협로 매핑 표.
- 미끼 C 회로 변종 3~4종(맵마다 다른 탐험 서사).

---

*v0.1 — 동심원→마름모 확정. 어케 만드냐 = 미션그래프→공간임베딩→지형+검증. 지형 = 도형이 아니라 전략·탐험 가치. 적 설계(§적_설계·패턴심화)와 맞물려 맵+적 블록 닫힘.*
