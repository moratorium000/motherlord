# MOTHERLODE — 절차생성 알고리즘 v0.1 (보장 우선 생성 파이프라인)

> 상위: `맵_탐험_설계_v0.1`(마름모·3단 개념·지형 7종·배치·검증). 본 문서 = 그 3단을 **구현 가능한 알고리즘**으로.
> 한 줄 원칙: **중요한 건 보장(불변식)으로 구조에 박고, 나머지만 무작위·최적화.** 진행·루트가 절차생성이어도 절대 안 깨지고, 목표(30분 곡선)에 맞고, 플레이를 방해하지 않게.

---

## 0. 설계 철학 — 보장 우선 (왜 순진한 절차생성은 망하나)

| 순진한 방식 | 문제 |
|---|---|
| 무작위 배치 → 나쁜 거 버리기(reject) | 느림, 가끔 무한 재시도, 던졌다 다시 만들어 낭비 |
| "거의 다 되겠지" 낙관 | 가끔 진행불가·소프트락·재미없는 맵을 **출하** |
| 한 방에 전부 무작위 | 난이도 역전·교착·죽음의 미로 등 플레이 방해 |

**해법(통설):** 제약 기반 생성은 *유효 출력 전부가 구성상 제약을 만족*하게 만들 수 있다. 그래서 속성을 **3티어**로 나눠 다루는 기법을 달리한다.

---

## 1. 3티어 속성 — 무엇을 보장하고 무엇을 흔드나

| 티어 | 성격 | 다루는 법 | 예 |
|---|---|---|---|
| **불변식(Invariant)** | 하드. 위반 = 출하 금지 | **구성상 보장** or 제약충족+검증 | 솔벌, 무소프트락, 난이도 단조, 공정 개막, 다중루트, 만회경로 |
| **목표(Objective)** | 소프트. 잘할수록 좋음 | **점수화 + 국소탐색 최적화** | 30분 곡선 정합, 탐험 풍부도, 지형 변주, 유인 배치 |
| **다양성(Diversity)** | 매번 달라야 | **제약 공간 내 무작위(시드)** | 모듈 선택, 정확 위치, 지형 디테일 |

> 핵심: 불변식을 *희망*하지 말고 *구조*로 만든다. 그러면 절차생성이어도 진행이 안 깨진다.

---

## 2. 파이프라인 — 단계별로 맞는 기법 (정답에서 출발 → CSP → WFC → 검증·수리)

> 단계마다 **그 단계에 맞는 생성 기법**을 쓴다(전부 한 솔버에 넣으면 폭발 — §7). 단계 분리가 검증·재시도를 싸게 만든다(통설).

### 2.0 [스파인] 진행 골격을 "이미 풀린 상태"에서 시작 — correct-by-construction

가장 중요한 결정. 미션 그래프를 자유 생성하고 솔벌을 *비는* 대신, **항상 유효한 진행 척추**에서 출발한다.

- A→B를 **단조 access-level의 선형 DAG**로 깖: `A(0) → 지역1(1) → 자물쇠1 → 지역2(2) → … → B(n+1)`.
- 이 선형 DAG는 **본질적으로 솔벌 가능**(위상 정렬됨) + **무소프트락**(열쇠/조건이 항상 잠금보다 낮은 access-level) + **난이도 단조** — **세 불변식이 공짜.**
- 자물쇠 = 미니보스(처치=통과, 별도 열쇠템 불필요 → "조건=상류 클리어"가 access-level로 자동 보장).

### 2.1 [미션 그래프] 불변식 보존 그래프 재작성

스파인에 가지·곁주머니·회로·만회로를 **불변식을 깨지 않는 규칙으로만** 덧붙인다(생성 규칙 + 완료가능성 보장 규칙).

- **측면 가지 규칙:** `지역i`에서 갈라져 **하류 `지역j(j>i)`로 반드시 재합류** → 다중루트(I5) 보장, 고아 노드 없음, DAG 유지.
- **곁주머니(유물 금고) 규칙:** 자물쇠 뒤 막다른 보상 노드. access-level = 자물쇠 레벨.
- **미끼 C 모듈(필수):** `문지기→유혹→성장→전설마커→우회→전설`을 한 모듈로 삽입 → 탐험(I9)·만회(I6) 보장.
- **잠금 배치 전제조건:** 규칙은 잠금을 둘 때 그 통과조건 노드가 **엄격히 낮은 access-level**일 때만 적용(열쇠가 잠금 뒤로 못 감).

### 2.2 [공간 임베딩] 제약 충족 레이아웃 (CSP + 무작위 순서)

논리 그래프를 마름모 좌표로. 백트래킹/완화 솔버.

- **하드 제약:** access-level → A→B 대각 좌표(단조) · Poisson 최소 간격 · 마름모 내부 · 필수 루트 회랑 폭 확보.
- **소프트:** 측면 L/R 분산 · 엣지 교차 최소 · 미적 분포.
- **다양성:** 변수·값 순서를 **시드 무작위화**(결정적 순서면 매번 같은 맵 — 통설 주의).

### 2.3 [지형 구체화] 루트 먼저 예약, 그 다음 경계 WFC

지형이 **필수 루트를 절대 막지 못하게** 순서를 못박는다.

1. **루트 회랑 예약:** 그래프 엣지 = 통행 보장 회랑으로 먼저 잠금(불가침).
2. **경계 WFC/셰이프 그래머:** 남은 공간에만 지형 채움 — 지역 역할(요충=협로, 격전지=개활, 미연결 경계=위험지형 자연벽) + **지형 변주 쿼터**(허리 넓은 개활지 ≥1, 협로 남용 상한) 준수.
3. 회랑은 지형 충전에서 제외 → "지형이 길을 막는" 사고 원천 차단.

### 2.4 [개체 배치] 제약 충족 포퓰레이션

거점·엘리트·미니보스(=그래프 자물쇠)·유물·토템을 §3 규칙 = 제약으로 배치(빠른 플레이어빌리티 제약 포퓰레이션).

- 유물 등급 ≤ 지역 access-level · 토템 ⊆ 교두보 · 거점은 협로/고지 뒤 · 전설유물은 자물쇠/위험지형 뒤.

### 2.5 [검증 + 수리 + 폴백] reject 말고 repair

```
fails = verify(map, INVARIANTS)        # 대부분 구성상 통과
budget = R
while fails and budget>0:
  f = worst(fails)
  if repairable(f): map = repair(map, f)   # 국소: 회랑 확장 / 노드 이동 / 엣지 추가
  else:             map = rerun(stageOf(f), reseed())  # 해당 단계만 재생성
  fails = verify(map, INVARIANTS); budget--
if fails: return AUTHORED_SAFE_TEMPLATE     # 절대 깨진 맵 출하 안 함(폴백)
map = optimizeObjectives(map)               # 소프트 목표 perturb-and-rescore
```

- **수리 우선:** 통째 버리지 않고 불가요인 지역만 국소 수정(경로탐색·MILP식). 빠르고 좋은 부분 보존.
- **폴백:** 예산 초과 시 손수 만든 안전 템플릿 — **깨진 맵 출하 0% 보장.**

---

## 3. 불변식 명세 — 진행이 안 깨지는 9 보장 (+ 보장 메커니즘)

| # | 불변식 | 보장 메커니즘 |
|---|---|---|
| **I1 솔벌** | A에서 보스 도달 가능 | 스파인 = 공짜(2.0) |
| **I2 무소프트락** | 모든 잠금 통과조건이 그 전에 충족 가능 | access-level 위상 순서 = 공짜 |
| **I3 난이도 단조** | access-level이 A→B 단조 ↑ | 스파인 = 공짜 + 포퓰 난이도 평활 패스 |
| **I4 공정 개막** | 최근접 밴드0 거점이 첫 교두보 전엔 A 못 깸 | 거리/각성 지연 제약(2.2·2.4) |
| **I5 다중 루트** | A→B 경로 ≥2(주축+측면) | 측면 가지 규칙이 하류 재합류 보장(2.1) |
| **I6 만회 경로** | 뒤처진 자용 고위험 지름길 ≥1 | 미끼 C 모듈 필수 삽입(2.1) |
| **I7 가독 진행** | 명확한 "앞" + 죽음의 미로 없음 | 굴곡 상한·노드 분기 팬아웃 상한 + 안개·비콘 북극성 |
| **I8 다중해(헌법8)** | 보스·핵심 자물쇠가 대표 빌드로 클리어 | 포퓰 제약 + 헤드리스 시뮬 검증(§8) |
| **I9 탐험 보증** | 미끼 C 존재·해결가능 + 유인 비콘 ≥1 | 미끼 C 모듈 필수 |

---

## 4. "플레이 방해 안 함" — 절차생성 실패모드 ↔ 가드 매핑

> 절차생성이 플레이를 망치는 방식은 정해져 있다. 각각에 가드를 단다.

| 실패모드(플레이 방해) | 가드 |
|---|---|
| 진행 막힘(소프트락·고립) | I1·I2 구성상 보장(스파인+위상순서) |
| 시작하자마자 압살(불공정 개막) | I4 거리/각성 제약 |
| 불가능·답답한 지형(미로·막힌 협로) | 루트 회랑 예약(2.3)·굴곡/팬아웃 상한(I7) |
| 난이도 스파이크·역전 | I3 단조 + 평활 패스 |
| 길 잃음(어디로 가야?) | 스파인의 명확한 "앞" + 안개 점진 + 보스 비콘(I7) |
| 천편일률 or 카오스 | 다양성(시드 무작위) ∩ 변주 쿼터(2.3) |
| 교착(루트 부족) | I5 ≥2 루트 |
| 데스 스파이럴(뒤처지면 끝) | I6 만회 경로 |
| 곡선 어긋남(콘텐츠가 페이싱과 불일치) | §5 목표 점수화 |

---

## 5. 목표 정합 — "유효한 일반 맵"이 아니라 "이 게임의 30분 곡선"

생성은 *유효*를 넘어 **MOTHERLODE의 목표**를 섬겨야 한다. 목표 점수 = 아래 합.

- **소울 개방 곡선:** 마름모 밴드 = 소울 1→4 개방 단계(기획서 §10). 밴드별 콘텐츠 양·강도가 소울 진행과 정합되도록 점수.
- **공격 페이싱:** 근거리 밴드 = 방어적(협로·교두보), 외곽 밴드 = 공세적(개활·멀티 전선). "수성→공세" 자동 전환.
- **전진 경제:** 자원토템을 주축 앞쪽 교두보에 → 전진 유인(귀중 지역이 앞으로 끈다).
- **빙의 깊이(§1.1):** 자물쇠·보스 아레나를 몸 전환이 보상받는 위치에(비행 부위·탱 페이즈 등).
- **탐험 풍부도:** 미끼 C 변종·전설마커 가시성·곁주머니 밀도.

> 목표 점수가 임계 미만이면 통째 재생성이 아니라 **국소 perturb-and-rescore**(노드 미세 이동·모듈 교체).

---

## 6. 핵심 의사코드

**스파인 (정답에서 출발):**
```
buildSpine(seed, bands):
  spine=[node(A, access=0)]
  for i in 1..bands:
    r=node(region_i, access=i); connect(spine.last, r, REGULAR); spine+=r
    if i<bands: lock=node(miniboss_i, access=i); connect(r, lock, UNLOCK)
  spine += node(B, access=bands+1)
  return spine        # 선형 DAG: I1·I2·I3 구성상 보장
```

**측면 가지 (불변식 보존):**
```
addFlank(spine, i, j):            # i<j, 둘 다 주축 노드
  chain = newNodes(access in [i+1 .. j-1])   # 단조 유지
  connect(spine[i], chain.first, REGULAR)
  connect(chain.last, spine[j], REGULAR)     # 하류 재합류 → I5, DAG 유지, 고아 없음
```

**검증·수리 루프:** §2.5 참조.

---

## 7. 성능 · 결정성 · 작가 통제

- **솔버 폭발 회피(통설):** 전역 SAT 금지. 불변식은 *구성상*(스파인) → 솔버 부담 0. CSP는 2.2 레이아웃·2.4 포퓰의 **국소·소규모**에만. 캐시·예산 상한.
- **결정성:** 시드 → 단계별 결정적. 단계별 재시드 가능(한 단계만 다시 굴림). 공유·디버그 용이.
- **작가 통제(mixed-initiative):** 밴드 수·모듈 가중치·지형 쿼터·난이도 목표 곡선을 디자이너가 지정 → 생성이 *그 안에서* 변주. 블랙박스 아님.

---

## 8. 헤드리스 검증 프로토콜 (회귀 방지)

- **대량 생성 검증:** N(예: 1000)개 맵을 시드 변화로 생성, **9 불변식 전수 자동 점검**. 위반율 0 목표(폴백 발동율도 로깅).
- **솔벌 시뮬(I8):** 대표 빌드 3종(관통/정화/CC)으로 각 맵 클리어 경로 존재 자동 확인.
- **곡선 적합 분포:** 목표 점수 분포·생성 시간 분포 리포트(꼬리 = 폴백 후보).
- 프로토 관례(헤드리스 무에러)와 동일 — 규칙 하나 바꾸면 N맵 재검증.

---

## 9. 다음

- 회로 모듈 라이브러리 실데이터(미끼 C 변종 3~4 + 측면·곁주머니 패턴).
- 셰이프 그래머 규칙 세트(지형 7종 × 지역 역할).
- 평활 패스·perturb 연산자 정의(목표 최적화 구체).
- 작가 통제 파라미터 스키마 + 안전 폴백 템플릿 3~4종.
- 보스 아레나 지형 처방(§1.4)과 결합.

---

*v0.1 — 보장 우선 생성. 불변식 9개를 구성상 박아 진행·루트가 절차생성이어도 안 깨지고(I1~I9), 실패모드마다 가드를 달고(§4), 일반 유효맵이 아니라 30분 곡선을 섬기게(§5). reject 아닌 repair + 폴백으로 깨진 맵 출하 0%.*
